
allprojects {
    afterEvaluate { project ->
        // Add idea plugin.
        project.apply plugin: "idea"

        cleanIdeaWorkspace {
            group = "IDE"
            description = "Cleans IDEA workspace."
        }
    }
}

